#lang setup/infotab
(define name "Dijkstra's algorithm")
(define release-notes
  (list))
(define repositories
  (list "4.x"))
(define blurb
  (list "An implementation of Dijkstra's algorithm"))
(define scribblings '(("dijkstra.scrbl" ())))
(define primary-file "main.ss")
(define categories '(datastructures))